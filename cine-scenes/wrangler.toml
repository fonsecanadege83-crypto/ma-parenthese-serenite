name = "cine-scenes-ia"
main = "worker.js"
compatibility_date = "2026-08-01"

# Stockage des vidéos partagées dans la galerie publique (optionnel)
[[r2_buckets]]
binding = "MEDIA"
bucket_name = "cine-scenes-media"

# Aucune clé ni secret requis : le rendu vidéo se fait dans le navigateur.
